// Function to generate placeholder image URLs
function getPlaceholderImage(width = 180, height = 320) {
    return `https://picsum.photos/${width}/${height}`;
}

// Function to generate movie banner placeholder (wider image)
function getBannerPlaceholder(width = 1200, height = 400) {
    return `https://picsum.photos/${width}/${height}`;
}